namespace Jacobi.Vst.Core
{
    /// <summary>
    /// Indicates at what precision audio is processed.
    /// </summary>
    public enum VstProcessPrecision
    {
        /// <summary>32 bit precision (float or single).</summary>
        Process32,
        /// <summary>64 bit precision (double).</summary>
        Process64,
    }
}
